baguetteBox.run('.gallery');

const search = new Filter('search','A picture of a field');

const search = new Filter('search','A picture of a lake');

const search = new Filter('search','A picture of a field');

const search = new Filter('search','A picture of a frozen lake');

const search = new Filter('search','A picture of a gorge');

const search = new Filter('search','A picture of a forest');

const search = new Filter('search','A picture of a green forest');

const search = new Filter('search','A picture of a desert');

const search = new Filter('search','A picture of a pathway');

const search = new Filter('search','A picture of a sunset');

const search = new Filter('search','A picture of a view');

const search = new Filter('search','A picture of a mountain');